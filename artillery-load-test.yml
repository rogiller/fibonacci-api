config:
  target: 'http://localhost:8090/'
  http:
    # Responses have to be sent within 1 seconds or the request will be aborted
    timeout: 1
  phases:
    - duration: 5
      arrivalRate: 1000
scenarios:
  - flow:
      - get:
          url: "http://localhost:8090/next"
